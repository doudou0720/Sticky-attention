<ely:MyWindow x:Class="StickyHomeworks.Views.CrashWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
           xmlns:ely="http://github.com/HelloWRC/ElysiaFramework"
           xmlns:views="clr-namespace:StickyHomeworks.Views"
           d:DataContext="{d:DesignInstance views:CrashWindow}"
           TextElement.Foreground="{DynamicResource MaterialDesignBody}"
           TextElement.FontWeight="Regular"
           TextElement.FontSize="13"
           TextOptions.TextFormattingMode="Ideal"
           TextOptions.TextRenderingMode="Auto"
           Background="{DynamicResource MaterialDesignPaper}"
           FontFamily="{StaticResource LXGWWenKaiScreen}"
           mc:Ignorable="d"
           Icon="/Assets/AppLogo.png"
           Closing="CrashWindow_OnClosed"
           Title="应用问题" Height="480" Width="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0">
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
                <Run Text="❌" Foreground="{DynamicResource PrimaryHueMidBrush}" FontSize="45" FontWeight="Bold"/>
                <LineBreak/>
                <TextBlock Grid.Row="0" TextWrapping="Wrap" Grid.ColumnSpan="2" Margin="0,15,0,0">
                <Run Text="应用发生了无法解决的问题……" Foreground="{DynamicResource PrimaryHueMidBrush}" FontSize="15" FontWeight="Bold" />
                </TextBlock>
                <LineBreak/>
                <LineBreak/>
                <Run Text="应用发生了无法解决的错误，请将此错误反馈至开发人员！"/>
            </TextBlock>
        </ScrollViewer>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox x:Name="TextBoxCrashInfo" Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                     Margin="0,10,0,10" AcceptsReturn="True" VerticalContentAlignment="Top" 
                     VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding CrashInfo, Mode=OneWay}" 
                     TextWrapping="Wrap" HorizontalScrollBarVisibility="Auto" />
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,10">
            <Button Style="{StaticResource MaterialDesignPaperButton}" Margin="0 0 6 0" Click="ButtonIgnore_OnClick">
                <ely:IconText Kind="Close" Text="忽略提示" IconMargin="3 0 0 0"/>
            </Button>
            <Button Style="{StaticResource MaterialDesignPaperButton}" Margin="0 0 6 0" Click="ButtonExit_OnClick">
                <ely:IconText Kind="ExitToApp" Text="退出应用" IconMargin="3 0 0 0"/>
            </Button>
            <Button Click="ButtonRestart_OnClick">
                <ely:IconText Kind="Restart" Text="重启应用" IconMargin="3 0 0 0"/>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="2" HorizontalAlignment="Left" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Style="{StaticResource MaterialDesignPaperButton}" Margin="0 0 6 0" Click="ButtonCopy_OnClick">
                <ely:IconText Kind="ContentCopy" Text="拷贝错误" IconMargin="3 0 0 0"/>
            </Button>
            <Button Style="{StaticResource MaterialDesignPaperButton}" Click="ButtonRecover_OnClick">
                <ely:IconText Kind="Eject" Text="回档至上一次" IconMargin="3 0 0 0"/>
            </Button>
        </StackPanel>
    </Grid>
</ely:MyWindow>