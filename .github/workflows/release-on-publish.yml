name: Build Release on Publish

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build release for'
        required: true

jobs:
  build-release:
    runs-on: windows-latest
    
    strategy:
      matrix:
        runtime: [win-x64, win-x86]
        selfContained: [true]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.release.tag_name || github.event.inputs.tag }}

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Get version from tag
      id: version
      run: |
        $tag = "${{ github.event.release.tag_name || github.event.inputs.tag }}"
        echo "tag=$tag" >> $GITHUB_OUTPUT
      shell: pwsh

    - name: Install dependencies
      run: dotnet restore

    - name: Calculate suffix
      id: suffix
      run: |
        if [ "${{ matrix.selfContained }}" = "false" ]; then
          echo "suffix=-no-runtime" >> $GITHUB_OUTPUT
        else
          echo "suffix=" >> $GITHUB_OUTPUT
        fi
      shell: bash

    - name: Build project
      run: |
        dotnet publish StickyHomeworks/StickyHomeworks.csproj -c Release -r ${{ matrix.runtime }} --self-contained ${{ matrix.selfContained }} -p:PublishSingleFile=true -o ./publish-${{ matrix.runtime }}${{ steps.suffix.outputs.suffix }}/

    - name: Create release archives
      run: |
        cd publish-${{ matrix.runtime }}${{ steps.suffix.outputs.suffix }}
        Compress-Archive -Path * -DestinationPath ../sticky-attentions-forked-${{ steps.version.outputs.tag }}-${{ matrix.runtime }}${{ steps.suffix.outputs.suffix }}.zip
      shell: pwsh

    - name: Upload assets to release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.version.outputs.tag }}
        files: |
          sticky-attentions-forked-${{ steps.version.outputs.tag }}-*.zip
        prerelease: ${{ github.event.release.prerelease || false }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}